export type Deposit = { id: number; codigo: string; nome: string; ativo: boolean; padrao: boolean; created_at: string; updated_at: string }
export type Balance = { produto_id: number; deposito_id: number; saldo: string | number; estoque_minimo: string | number; abaixo_do_minimo: boolean }
export type Movement = { id: number; produto_id: number; deposito_id: number; tipo: string; quantidade: string | number; data_movimentacao: string; origem: string; documento_tipo: string | null; documento_id: number | null; movimento_origem_id: number | null; usuario_id: number | null; observacao: string | null; chave_idempotencia: string | null; created_at: string }
export type Inventory = { id: number; deposito_id: number; data_inventario: string; status: 'RASCUNHO' | 'CONFIRMADO' | 'CANCELADO'; usuario_id: number | null; observacao: string | null; created_at: string; updated_at: string; itens: { id: number; produto_id: number; saldo_sistema: string | number; quantidade_contada: string | number; diferenca: string | number }[] }
